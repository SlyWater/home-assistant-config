esphome:
  name: kitchen-ht-sensor
  friendly_name: Climate sensor

esp8266:
  board: d1_mini

logger:

api:

ota:
  platform: esphome

wifi:
  # networks:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
    # - ssid: !secret wifi_5g_ssid
      # password: !secret wifi_5g_password
  min_auth_mode: WPA2
  fast_connect: true
  manual_ip:
    # Set this to the IP of the ESP
    static_ip: 192.168.1.242
    # Set this to the IP address of the router. Often ends with .1
    gateway: 192.168.1.1
    # The subnet of the network. 255.255.255.0 works for most home networks.
    subnet: 255.255.255.0

# --- I2C для OLED ---
# i2c:
#   sda: D2
#   scl: D1
#   scan: true

# # --- Шрифт ---
# font:
#   - file: "gfonts://Roboto"
#     id: font_small
#     size: 12

#   - file: "gfonts://Roboto"
#     id: font_large
#     size: 24

# --- Дисплей ---
# display:
#   - platform: ssd1306_i2c
#     model: "SSD1306 128x64"
#     address: 0x3C
#     lambda: |-
#       it.printf(0, 0, id(font_small), "Temperature:");

#       if (id(temp).has_state()) {
#         it.printf(0, 18, id(font_large), "%.1f°C", id(temp).state);
#       }

#       if (id(hum).has_state()) {
#         it.printf(0, 46, id(font_small), "Humidity: %.0f%%", id(hum).state);
#       }

# --- Датчик DHT11 ---
sensor:
  - platform: dht
    model: DHT11
    pin: D5
    temperature:
      name: "Температура"
      id: temp
    humidity:
      name: "Влажность"
      id: hum
    update_interval: 60s
